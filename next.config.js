/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ["source.unsplash.com", "laugh-consonant.pockethost.io", "randomuser.me", "images.unsplash.com"]
    },
  };
  
  module.exports = nextConfig;
